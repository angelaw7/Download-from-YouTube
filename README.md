# YouTube-to-mp4

### .env configuration
```
CONFIG_KEY=<config_key>
SAVE_PATH=<save_path>
```

### Setup
- Create .env file with variables listed above
- Run ```pip install -r requirements.txt```
- Run ```flask run```
